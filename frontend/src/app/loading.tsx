// Единственная полноэкранная загрузка — SplashScreen в ClientLayout.
// При навигации не показываем второй экран, контент подставится по готовности.
export default function Loading() {
  return null;
}
